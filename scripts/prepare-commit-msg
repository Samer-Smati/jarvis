#!/bin/sh
# Strip Cursor co-author trailers injected by the IDE/Agent.
COMMIT_MSG_FILE=$1
if [ -f "$COMMIT_MSG_FILE" ]; then
  grep -v 'Co-authored-by: Cursor <cursoragent@cursor.com>' "$COMMIT_MSG_FILE" > "$COMMIT_MSG_FILE.tmp" || true
  mv "$COMMIT_MSG_FILE.tmp" "$COMMIT_MSG_FILE"
fi
