# @huggingface/transformers pulls onnxruntime-node; skip optional CUDA/GPU NuGet downloads.
# CPU binaries are bundled in the npm package — sufficient for Whisper STT on desktop.
onnxruntime_node_install_cuda=skip
